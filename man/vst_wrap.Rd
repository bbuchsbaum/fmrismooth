% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vst_wrap.R
\name{vst_wrap}
\alias{vst_wrap}
\title{Wrap a denoiser with a Rician VST (variance-stabilizing transform)}
\usage{
vst_wrap(x, sigma = NULL, denoise_fun, ...)
}
\arguments{
\item{x}{3D/4D magnitude array or neuroim2 volume/vec.}

\item{sigma}{Optional noise sigma. If `NULL` and a 4D input is provided,
sigma is estimated with `estimate_sigma_rician`.}

\item{denoise_fun}{Function that takes an array and returns an array of the
same dimensions.}

\item{...}{Additional arguments forwarded to `denoise_fun`.}
}
\value{
The denoised data, same shape/type as `x` (wrapped via `nv_wrap_like`).
}
\description{
Converts magnitude data to approximately homoscedastic Gaussian via VST,
applies `denoise_fun`, then inverts the VST.
}
